[build-system]
requires = [
  "setuptools ~= 74.1",
  "wheel ~= 0.44",
  "pyTooling ~= 6.5"
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120
target-version = ['py39', 'py310', 'py311', 'py312']
