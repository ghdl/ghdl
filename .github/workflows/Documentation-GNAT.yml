name: Auto-generating Ada Documentation (GNATdoc)

on:
  workflow_call:

jobs:
  Documentation:
    name: '📚 GNATdoc'
    runs-on: ubuntu-latest

    steps:
      - name: '🧰 Checkout'
        uses: actions/checkout@v4
